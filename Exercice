Créez un nouveau service UserService dans le dossier Service, dont la seule méthode getWelcomeMessage()
(outre le constructeur) se charge de renvoyer un message de bienvenue avec le prénom, puis le nom de l'utilisateur,
associés à l'id passé en paramètre (exemple : « Bienvenue Chris Chevalier »).

Créez une classe de test UserServiceTest dont la seule méthode testGetWelcomeMessage() permet de tester la cohérence
de la méthode getWelcomeMessage() à l'aide des mocks et stubs.